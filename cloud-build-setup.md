# 🌩️ Сборка IPA через облачные сервисы

Поскольку у вас нет Mac, вот несколько способов получить готовый IPA файл:

## Способ 1: GitHub Actions (Рекомендуется) ✅

### Преимущества:
- **Бесплатно** для публичных репозиториев
- Автоматическая сборка
- Не требует установки ПО

### Инструкция:
1. **Создайте репозиторий на GitHub**
   ```bash
   cd "ios app"
   # Windows:
   setup-github-build.bat
   # или вручную:
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **Создайте репозиторий на GitHub.com**
   - Перейдите на https://github.com/new
   - Назовите его, например: `support-chat-ios`
   - Сделайте PUBLIC (для бесплатной сборки)

3. **Загрузите код**
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/support-chat-ios.git
   git branch -M main
   git push -u origin main
   ```

4. **Запустите сборку**
   - Перейдите в Actions → Build iOS IPA for TrollStore
   - Нажмите "Run workflow"
   - Укажите IP вашего сервера (опционально)

5. **Скачайте IPA**
   - После сборки (~10-15 минут)
   - Actions → Выберите последний запуск
   - Внизу в Artifacts скачайте `SupportChat-TrollStore-IPA`

## Способ 2: Codemagic (Альтернатива) 🚀

### Преимущества:
- 500 минут бесплатно каждый месяц
- Простая настройка
- Поддержка приватных репозиториев

### Инструкция:
1. Зарегистрируйтесь на https://codemagic.io
2. Подключите GitHub репозиторий
3. Создайте файл `codemagic.yaml`:

```yaml
workflows:
  ios-workflow:
    name: iOS TrollStore Build
    max_build_duration: 60
    environment:
      xcode: latest
      cocoapods: default
    scripts:
      - name: Build for TrollStore
        script: |
          cd "ios app/SupportChat"
          xcodebuild -project SupportChat.xcodeproj \
            -scheme SupportChat \
            -configuration Release \
            -sdk iphoneos \
            -arch arm64 \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            clean build
      - name: Create IPA
        script: |
          cd "ios app/SupportChat"
          APP_PATH=$(find . -name "*.app" -type d | head -n 1)
          mkdir -p Payload
          cp -r "$APP_PATH" Payload/
          zip -r SupportChat-TrollStore.ipa Payload
    artifacts:
      - ios app/SupportChat/*.ipa
```

## Способ 3: Локальная macOS VM 💻

Если хотите полный контроль:

1. **Установите VMware/VirtualBox**
2. **Скачайте macOS образ** (поищите "macOS Sonoma ISO")
3. **Создайте VM с 8GB RAM минимум**
4. **Установите Xcode из App Store**
5. **Соберите проект как описано в инструкциях**

## Способ 4: Аренда Mac в облаке ☁️

### MacStadium / MacinCloud
- От $1/час
- Полноценный Mac через браузер
- Xcode уже установлен

### Scaleway Mac mini
- €0.10/час
- Доступ через VNC
- Подходит для разовой сборки

## 🎯 Быстрый старт с GitHub Actions

Я уже создал все необходимые файлы! Просто:

1. **Запустите в папке `ios app`:**
   ```cmd
   setup-github-build.bat
   ```

2. **Создайте репозиторий на GitHub и загрузите:**
   ```cmd
   git remote add origin https://github.com/ВАШ_ЛОГИН/НАЗВАНИЕ_РЕПО.git
   git push -u origin main
   ```

3. **IPA будет готов через 10-15 минут!**

## 📱 После получения IPA

1. Перенесите на iPhone (AirDrop, Telegram, Email)
2. Откройте в TrollStore
3. Установите
4. Запустите и проверьте подключение к серверу

## ⚙️ Настройка IP сервера

При сборке через GitHub Actions можно указать IP:
- Actions → Run workflow → Server IP
- Или измените в коде перед загрузкой

---

💡 **Совет**: GitHub Actions - самый простой и бесплатный способ. Займет всего 5 минут на настройку!
