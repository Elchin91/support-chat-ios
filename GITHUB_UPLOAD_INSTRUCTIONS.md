# 🚀 Инструкция: Получение готового IPA файла через GitHub

## ✅ Что уже сделано:
- iOS приложение создано и готово к сборке
- GitHub Actions настроен для автоматической сборки
- Git репозиторий инициализирован локально

## 📋 Шаги для получения IPA:

### 1️⃣ Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. Заполните:
   - **Repository name**: `support-chat-ios` (или любое другое)
   - **Public/Private**: выберите **Public** (для бесплатной сборки)
   - НЕ инициализируйте с README
3. Нажмите **Create repository**

### 2️⃣ Загрузите код

Скопируйте команды с страницы GitHub или используйте эти (замените YOUR_USERNAME):

```bash
cd "C:\Users\elchi\Desktop\support-chat\ios app"
git remote add origin https://github.com/YOUR_USERNAME/support-chat-ios.git
git branch -M main
git push -u origin main
```

### 3️⃣ Запустите сборку

#### Вариант А: Автоматически
Сборка запустится автоматически после загрузки кода.

#### Вариант Б: Вручную с указанием IP сервера
1. Перейдите в репозиторий на GitHub
2. Вкладка **Actions**
3. Слева выберите **Build iOS IPA for TrollStore**
4. Справа нажмите **Run workflow**
5. В поле **Backend server IP** введите IP вашего компьютера (например: 192.168.1.100)
6. Нажмите зеленую кнопку **Run workflow**

### 4️⃣ Дождитесь сборки (10-15 минут)

1. Обновляйте страницу Actions
2. Кликните на запущенный workflow
3. Наблюдайте за прогрессом

### 5️⃣ Скачайте IPA

1. После успешной сборки (зеленая галочка ✓)
2. Прокрутите вниз до секции **Artifacts**
3. Скачайте **SupportChat-TrollStore-IPA**
4. Распакуйте ZIP - внутри будет IPA файл

## 📱 Установка на iPhone

1. Перенесите IPA на iPhone любым способом:
   - **AirDrop** (если есть Mac)
   - **Telegram** (отправьте себе в Избранное)
   - **Google Drive** / **Dropbox**
   - **Email**

2. Откройте TrollStore
3. Нажмите "+" 
4. Выберите IPA файл
5. Нажмите Install

## ⚙️ Настройка сервера в приложении

### Найдите IP вашего компьютера:
```cmd
ipconfig
```
Ищите IPv4 Address (например: 192.168.1.100)

### Убедитесь что:
- Backend запущен: `npm start` в папке support-chat
- Компьютер и iPhone в одной Wi-Fi сети
- Firewall разрешает подключения на порт 3000

## 🔧 Изменение IP сервера

### Вариант 1: При сборке
Указывайте IP при запуске workflow в GitHub Actions

### Вариант 2: В коде перед загрузкой
1. Откройте `SupportChat/SupportChat/Config/AppConfig.swift`
2. Измените строку:
   ```swift
   static let serverHost = "192.168.1.100" // Ваш IP
   ```
3. Сделайте коммит и push:
   ```bash
   git add .
   git commit -m "Update server IP"
   git push
   ```

## 🎯 Быстрые команды

### Первая загрузка (замените YOUR_USERNAME):
```bash
git remote add origin https://github.com/YOUR_USERNAME/support-chat-ios.git
git push -u origin main
```

### Последующие обновления:
```bash
git add .
git commit -m "Описание изменений"
git push
```

## ❓ Частые проблемы

### "Permission denied" при push
- Используйте Personal Access Token вместо пароля
- Создайте на: https://github.com/settings/tokens

### Сборка провалилась
- Проверьте вкладку Actions → кликните на failed build
- Посмотрите логи для деталей ошибки

### IPA не устанавливается
- Убедитесь что используете TrollStore
- Проверьте совместимость версии iOS

## 🎉 Готово!

После установки:
1. Откройте приложение Support Chat
2. Перейдите на вкладку AI (по центру)
3. Начните общаться с ассистентом!

---

💡 **Совет**: Сохраните ссылку на репозиторий - вы сможете скачать IPA в любое время из раздела Actions → Artifacts (хранится 30 дней)
