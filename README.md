# Support Chat iOS App

iOS приложение для Support Chat с интегрированным AI ассистентом.

## Описание

Это нативное iOS приложение, созданное на SwiftUI, которое предоставляет:
- 💳 Управление балансом и платежами
- 💬 Встроенный AI чат ассистент
- 📱 Интеграцию с существующим backend через WebSocket и REST API
- 🎨 Современный дизайн согласно предоставленным макетам

## Структура приложения

### Экраны (TabView с 5 вкладками):
1. **Главная** - Баланс, быстрые действия, сервисы
2. **Платежи** - Каталог услуг и сохраненные платежи  
3. **AI** (центральная вкладка) - Чат с AI ассистентом
4. **История** - История транзакций
5. **Профиль** - Настройки и информация пользователя

## Требования

- iOS 16.0+
- Xcode 15.0+
- Swift 5.9+

## Установка и запуск

### 1. Откройте проект в Xcode

```bash
cd "ios app/SupportChat"
open SupportChat.xcodeproj
```

### 2. Настройка подключения к серверу

По умолчанию приложение подключается к `localhost:3000`. Чтобы изменить:

1. Откройте схему запуска в Xcode (Product → Scheme → Edit Scheme)
2. Добавьте переменные окружения:
   - `SERVER_HOST`: IP адрес сервера (например, `192.168.1.100`)
   - `SERVER_PORT`: Порт сервера (по умолчанию `3000`)

### 3. Запустите backend сервер

Убедитесь, что основной сервер запущен:

```bash
cd ../.. # вернуться в корень проекта
npm start
```

### 4. Запустите приложение

1. Выберите симулятор или подключенное устройство
2. Нажмите Run (Cmd+R)

## Основные возможности

### AI Чат
- Real-time общение через WebSocket
- Fallback на REST API при отсутствии WebSocket соединения
- Индикатор набора текста
- История сообщений

### Интеграция с Backend
- Автоматическое подключение при запуске
- Переподключение при потере связи
- Синхронизация состояния через WebSocket

### Безопасность
- Поддержка Face ID/Touch ID
- Безопасное хранение сессии
- Шифрованная передача данных

## Конфигурация

Основные настройки находятся в файле `Config/AppConfig.swift`:
- URL сервера
- Таймауты подключения
- UI настройки
- API endpoints

## Архитектура

- **MVVM** паттерн для организации кода
- **Combine** для реактивного программирования
- **SwiftUI** для UI
- **SocketIO** для real-time связи

## Тестирование на устройстве

1. Убедитесь, что устройство и компьютер в одной сети
2. Используйте IP адрес компьютера вместо localhost
3. Проверьте, что порт 3000 доступен в сети

## Примечания

- Приложение использует современные iOS API и требует iOS 16+
- WebSocket соединение автоматически переподключается при потере связи
- AI чат сохраняет контекст беседы в рамках сессии

## Поддержка

При возникновении проблем:
1. Проверьте консоль Xcode на наличие ошибок
2. Убедитесь, что backend сервер запущен
3. Проверьте сетевое подключение
